function tabla_Del_seis(){
	let tabla=[];// creando una lista

	for(let i=0;i<=10;i=i+1){ // se le pone let porque permite declarar variables limitando un alcanse
		tabla.push(6*i); // añade uno o mas elementos a un array
	}
	return tabla;
}